import { AnunciofiltroPipe } from './anunciofiltro.pipe';

describe('AnunciofiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new AnunciofiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
